spring:
  application:
    name: store
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS}
        metadata:
          version: V1.0.0
      config:
        server-addr: ${NACOS}
        shared-dataids: store.yml
  main:
    allow-bean-definition-overriding: true
        
