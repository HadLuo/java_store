spring:
  application:
    name: store
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_CONFIG_ADDR}
        metadata:
          version: V1.0.0
      config:
        server-addr: ${NACOS_CONFIG_ADDR}
        shared-dataids: store.yml
  main:
    allow-bean-definition-overriding: true
        
